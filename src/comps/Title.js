import React from 'react';

const Title = () => {
  return (
    <div className="title">
      <h1>Image Up</h1>
      <h2>Your Images</h2>
      <p>Click on an image to enlarge it (if running locally, you can also see its labels).</p>
    </div>
  )
}

export default Title;